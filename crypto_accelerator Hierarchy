crypto_accelerator
  + instr    (IF)
    + pc    (programCounter)
    + instructionMem    (instructmem)
    + instructionDecode    (instrDec)
  + rf    (RF)
    + cl    (controlLogic)
    + regFile    (registerFile)
  + ex    (EXEC)
    + alu    (ALU)
    + aes    (aesUnit)
      + enc    (aesEnc)
        + sByte1    (subBytes)
          + subB    (sboxSb)
        + sRow1    (shiftRows)
        + mCol    (mixColumns_col)
          + times2_instance    (xTimes2)
          + times3_instance    (xTimes3)
            + times2    (xTimes2)
      + dec    (aesDec)
        + invSR    (invShiftRows)
        + invSbox    (sboxInvSb)
        + mCol    (invMixColumns_col)
          + timesE_instance    (xTimese)
            + u1    (xTimes2)
            + u2    (xTimes2)
            + u3    (xTimes2)
          + times9_instance    (xTimes9)
            + u1    (xTimes2)
            + u2    (xTimes2)
            + u3    (xTimes2)
          + timesD_instance    (xTimesd)
            + u1    (xTimes2)
            + u2    (xTimes2)
            + u3    (xTimes2)
          + timesB_instance    (xTimesb)
            + u1    (xTimes2)
            + u2    (xTimes2)
            + u3    (xTimes2)
      + keyExpand    (keyExpansion)
        + subWd    (subWord)
          + sub    (Sbox)
        + rcon_i    (rcon)
